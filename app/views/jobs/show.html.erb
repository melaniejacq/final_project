<div class="page-header">
  <h3><%= @job.title %></h3>
</div>

<div class="row">
  <div class="col-md-12 mb-2">
    <dl class="dl-horizontal">
      <!--<dt>Title</dt>-->
      <!--<dd><#%= @job.title %></dd>-->

      <dt>Company</dt>
      <dd><%= @job.company.name %></dd>

      <dt>Location</dt>
      <dd><%= @job.location %></dd>

      <dt>Job type</dt>
      <dd><%= @job.job_type.name %></dd>

    </dl>

    <div class="btn-group btn-group-justified">
      <a href="/jobs" class="btn btn-primary">
        Back
      </a>
      <a href="/jobs/<%= @job.id %>/edit" class="btn btn-warning">
        Edit
      </a>
      <a href="/delete_job/<%= @job.id %>" class="btn btn-danger" rel="nofollow">
        Delete
      </a>
    </div>
    
    <!--<ul>-->
    <!--<#% @job.job_type.job_type_matches.each do |match| %>-->
    <!--<li><#%= @job.job_type.job_type_matches.candidate_id.email %></li>-->
    <!--<#% end %>-->
    <!--</ul>-->
    
  </div>
</div>
